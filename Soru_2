def toplama(N):
    if N<0:
        return "Pozitif sayı giriniz."
    elif N == 0:
        return 0
    else:
        if N == 1:
            return 1
        else:
            return N+toplama(N-1)
